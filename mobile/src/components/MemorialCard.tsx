import React from 'react'; import {Image,Pressable,StyleSheet,Text,View} from 'react-native'; import {useTranslation} from 'react-i18next'; import type {Memorial} from '../types'; import {colors} from './ui';
export function MemorialCard({item,onPress}:{item:Memorial;onPress:()=>void}){const {t}=useTranslation();return <Pressable style={s.card} onPress={onPress}><Image source={{uri:item.photo}} style={s.image}/><View style={s.text}><Text style={s.name}>{item.fullName}</Text><Text style={s.years}>{item.birthDate.slice(0,4)} — {item.deathDate.slice(0,4)}</Text><Text style={[s.status,item.status==='approved'&&s.approved,item.status==='rejected'&&s.rejected]}>{t(item.status)}</Text></View><Text style={s.arrow}>›</Text></Pressable>}
const s=StyleSheet.create({card:{backgroundColor:'#fff',borderWidth:1,borderColor:colors.line,padding:10,flexDirection:'row',alignItems:'center',marginBottom:10,borderRadius:7},image:{width:66,height:76,borderRadius:4},text:{flex:1,paddingHorizontal:13},name:{fontSize:17,fontFamily:'serif',fontWeight:'600',color:colors.ink},years:{color:colors.muted,marginTop:4},status:{fontSize:10,textTransform:'uppercase',color:'#86651e',marginTop:8,fontWeight:'700'},approved:{color:'#326343'},rejected:{color:colors.danger},arrow:{fontSize:28,color:colors.gold}});
