import React from'react';import{Alert,Linking,Share,StyleSheet,Text}from'react-native';import Clipboard from'@react-native-clipboard/clipboard';import{useTranslation}from'react-i18next';import type{NativeStackScreenProps}from'@react-navigation/native-stack';import{MemorialPreview}from'../components/MemorialPreview';import{Button,Screen,colors}from'../components/ui';import type{RootStackParams}from'../types';
export function MemorialDetailScreen({route}:NativeStackScreenProps<RootStackParams,'MemorialDetail'>){const{t}=useTranslation();const item=route.params.memorial;const url=item.slug?`https://your-domain.com/memorial/${item.slug}`:'';const text=`${t('inMemory')} ${item.fullName}. ${url}`;return <Screen scroll><MemorialPreview item={item}/><Text style={s.status}>{t(item.status)}</Text>{item.status==='approved'&&<><Button title={t('share')} onPress={()=>void Share.share({message:text,url})}/><Button title={t('facebook')} kind="outline" onPress={()=>void Linking.openURL(`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`)}/><Button title={t('whatsapp')} kind="outline" onPress={()=>void Linking.openURL(`https://wa.me/?text=${encodeURIComponent(text)}`)}/><Button title={t('copy')} kind="outline" onPress={()=>{Clipboard.setString(url);Alert.alert(t('copied'))}}/></>}</Screen>}
const s=StyleSheet.create({status:{textAlign:'center',textTransform:'uppercase',letterSpacing:2,color:colors.gold,fontWeight:'700',marginVertical:15}});
